#include<stdio.h>
#include <string.h>

main(){
	//文字列とかforのためのiを用意
	char WM[] = "a*b-cd/e+fg";////ごーる→gfedcba+-*/
	char on;
	int i;

	////////////メモ//////文字列は%s,一文字は%cで表示	printf("%c\n", WM[1]);
	//////////ルール1開始//////////
	printf("ルール1：「/」は文字列の一番右の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i] == '/'){
			//////////onに/と交換する右の文字を入れて、/に上書き。/の元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = '/';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール2開始//////////
	printf("ルール2：「*」は/の左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == '/'){
			break;
		}
		if (WM[i] == '*'){
			//////////onに*と交換する右の文字を入れて、*に上書き。*の元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = '*';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール3開始//////////
	printf("ルール3：「-」は*の左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == '*'){
			break;
		}
		if (WM[i] == '-'){
			//////////onに-と交換する右の文字を入れて、-に上書き。-の元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = '-';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール4開始//////////
	printf("ルール4：「+」は-の左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == '-'){
			break;
		}
		if (WM[i] == '+'){
			//////////onに+と交換する右の文字を入れて、+に上書き。+の元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = '+';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール5開始//////////
	printf("ルール5：「a」は+の左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == '+'){
			break;
		}
		if (WM[i] == 'a'){
			//////////onにaと交換する右の文字を入れて、aに上書き。aの元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = 'a';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール6開始//////////
	printf("ルール6：「b」はaの左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == 'a'){
			break;
		}
		if (WM[i] == 'b'){
			//////////onにbと交換する右の文字を入れて、bに上書き。bの元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = 'b';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール7開始//////////
	printf("ルール7：「c」はbの左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == 'b'){
			break;
		}
		if (WM[i] == 'c'){
			//////////onにcと交換する右の文字を入れて、cに上書き。cの元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = 'c';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール8開始//////////
	printf("ルール8：「d」はcの左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == 'c'){
			break;
		}
		if (WM[i] == 'd'){
			//////////onにdと交換する右の文字を入れて、dに上書き。dの元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = 'd';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール9開始//////////
	printf("ルール9：「e」はdの左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == 'd'){
			break;
		}
		if (WM[i] == 'e'){
			//////////onにeと交換する右の文字を入れて、eに上書き。eの元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = 'e';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール10開始//////////
	printf("ルール10：「f」はeの左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == 'e'){
			break;
		}
		if (WM[i] == 'f'){
			//////////onにfと交換する右の文字を入れて、fに上書き。fの元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = 'f';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	//////////ルール11開始//////////
	printf("ルール11：「g」はfの左の文字である\n");
	for (i = 0; i < 10; i++){
		if (WM[i + 1] == 'f'){
			break;
		}
		if (WM[i] == 'g'){
			//////////onにgと交換する右の文字を入れて、gに上書き。gの元の場所にonを入れる//////
			on = WM[i + 1];
			WM[i + 1] = 'g';
			WM[i] = on;
			//表示//
			printf("%s\n", WM);
		}
	}

	printf("完了しました\n");

}